# =============================================================================
# CONFIGURAÇÕES DOCKER - CURIANGO CONTROLE DE ATIVOS
# =============================================================================
# Arquivo de exemplo das variáveis de ambiente para containers Docker
# Copie este arquivo para .env e ajuste os valores conforme necessário

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES GERAIS DO FLASK
# -----------------------------------------------------------------------------

# Ambiente de execução: development, production ou testing
FLASK_ENV=production

# Chave secreta para criptografia de sessões (ALTERE EM PRODUÇÃO)
SECRET_KEY=curiango-prod-secret-change-me

# Chave secreta para tokens JWT (ALTERE EM PRODUÇÃO)  
JWT_SECRET_KEY=curiango-jwt-secret-change-me

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DO BANCO DE DADOS
# -----------------------------------------------------------------------------

# String de conexão usando hostname do container Docker
SQLALCHEMY_DATABASE_URI=mysql+pymysql://usuario_app:senha_segura@db:3306/sistema_ativos_db

# Exibir consultas SQL no log (true/false) - usar false em produção
SQLALCHEMY_ECHO=false

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE EMAIL (SMTP)
# -----------------------------------------------------------------------------

# Servidor SMTP para envio de emails
MAIL_SERVER=mail.wgo.com.br

# Porta do servidor SMTP (587 para TLS, 465 para SSL, 25 para sem criptografia)
MAIL_PORT=587

# Usar criptografia TLS (true/false)
MAIL_USE_TLS=true

# Credenciais do usuário SMTP
MAIL_USERNAME=noreply@wgo.com.br
MAIL_PASSWORD=0PaEl5n%xPSGaz

# Email remetente padrão para notificações do sistema
MAIL_DEFAULT_SENDER=noreply@wgo.com.br

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES LDAP/ACTIVE DIRECTORY
# -----------------------------------------------------------------------------

# Endereço IP ou hostname do servidor LDAP/Active Directory
LDAP_HOST=192.168.22.222

# Domínio do Active Directory para autenticação (formato: dominio.local)
LDAP_DOMAIN=wgo.intranet

# Base DN para busca de usuários no LDAP (onde estão os usuários na árvore)
LDAP_BASE_DN=OU=WGO,DC=wgo,DC=intranet

# Grupos do AD que têm permissão de acesso ao sistema (separados por vírgula)
# Usuários devem pertencer a pelo menos um destes grupos
LDAP_ALLOWED_GROUPS=GG_Infra,Suporte_Rede_Interno

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE LOGS
# -----------------------------------------------------------------------------

# Nível de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Arquivo de log (será mapeado para volume Docker)
LOG_FILE=/app/logs/app.log